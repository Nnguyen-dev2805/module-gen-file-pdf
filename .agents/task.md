# Task Checklist: Vietnamese PDF Generation & Validation

This checklist reflects the current OpenCode-only implementation.

## Phase 1: Environment & Mock Architecture

- `[x]` Create `requirements.txt` with Typst, Pillow, OpenCV, pdfplumber, Flask, python-dotenv, and requests.
- `[x]` Create deterministic mock OCR and translation adapters in `src/mock_adapters.py`.
- `[x]` Bundle local fonts for Vietnamese PDF rendering.

## Phase 2: OpenCode LLM Integration

- `[x]` Create `src/llm_client.py` with `OpenCodeClient`.
- `[x]` Use OpenCode chat completions for Vietnamese Markdown report generation.
- `[x]` Use OpenCode chat completions for diagram label translation.
- `[x]` Support mock fallback when `OPENCODE_API_KEY` is missing.
- `[x]` Configure timeout through `OPENCODE_TIMEOUT`, defaulting to `300` seconds.

## Phase 3: Dedicated PDF Image Section

- `[x]` Create `src/report_image.py`.
- `[x]` Use `temp/example.jpg` as the fixed source image for PDF image output.
- `[x]` Render `temp/report_example_translated.png` with Vietnamese label overlays.
- `[x]` Strip image tags generated by the LLM.
- `[x]` Append a dedicated image section to Markdown before Typst compilation.
- `[x]` Keep `src/image_translator.py` for legacy helper tests.

## Phase 4: Typst PDF Generation

- `[x]` Create `src/typst_generator.py`.
- `[x]` Convert basic Markdown elements to Typst.
- `[x]` Convert Markdown tables to Typst tables.
- `[x]` Compile Vietnamese PDF with local bundled fonts.

## Phase 5: Validation & Rescue

- `[x]` Extract PDF text with pdfplumber.
- `[x]` Check for U+FFFD replacement characters.
- `[x]` Check for known leaked English diagram labels.
- `[x]` Implement 4-tier rescue pipeline:
  - NFC normalization.
  - Fallback fonts.
  - Simplified layout.
  - Plain-text fallback.

## Phase 6: Interfaces

- `[x]` Create CLI demo in `src/demo.py`.
- `[x]` Create Flask backend in `src/app.py`.
- `[x]` Create dashboard frontend in `src/templates/index.html`.

## Phase 7: Tests

- `[x]` Unit tests for OpenCode client mock/fallback behavior.
- `[x]` Unit tests for mock adapters.
- `[x]` Unit tests for image translator.
- `[x]` Unit tests for Typst generator.
- `[x]` Unit tests for PDF validator.
- `[x]` Unit tests for rescue pipeline.
- `[x]` Optional live OpenCode integration tests, skipped when `OPENCODE_API_KEY` is missing.
